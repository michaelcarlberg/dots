#!/usr/bin/env bash

printf -- "[1;32m[local][0m\n"
(cd /tmp/.X11-unix && for x in X*; do echo ":${x#X}"; done)

printf -- "[1;32m[remote][0m\n"
sudo netstat -lnt | awk '
  sub(/.*:/,"",$4) && $4 >= 6000 && $4 < 6100 {
    print ($1 == "tcp6" ? "ip6-localhost:" : "localhost:") ($4 - 6000)
  }'
